fn main() -> i32 {
    let arr: [i32; 3] = [10, 20, 30];

    let first: i32 = arr[0];
    if first != 10 { return 1; }

    let third: i32 = arr[2];
    if third != 30 { return 2; }

    let sum: i32 = arr[0] + arr[1] + arr[2];
    if sum != 60 { return 3; }

    0
}
