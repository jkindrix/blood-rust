fn apply(f: fn(i32) -> i32, x: i32) -> i32 {
    f(x)
}

fn main() -> i32 {
    let dbl: fn(i32) -> i32 = |x| x * 2;
    let a: i32 = dbl(3);
    if a != 6 { return 1; }

    let c: i32 = apply(|x| x * x, 5);
    if c != 25 { return 2; }

    0
}
