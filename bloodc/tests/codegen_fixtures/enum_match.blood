enum Shape {
    Circle(i32),
    Square(i32),
}

fn area_approx(s: Shape) -> i32 {
    match s {
        Shape::Circle(r) => r * r * 3,
        Shape::Square(side) => side * side,
    }
}

fn main() -> i32 {
    let c = Shape::Circle(5);
    area_approx(c)
}
