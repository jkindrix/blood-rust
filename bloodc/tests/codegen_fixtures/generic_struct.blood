struct Wrapper<T> {
    value: T,
}

fn unwrap_int(w: Wrapper<i32>) -> i32 {
    w.value
}

fn main() -> i32 {
    let w: Wrapper<i32> = Wrapper { value: 42 };
    let v: i32 = unwrap_int(w);
    if v != 42 { return 1; }
    0
}
