fn apply(f: fn(i32) -> i32, x: i32) -> i32 {
    f(x)
}

fn double(x: i32) -> i32 {
    x * 2
}

fn add_two(x: i32) -> i32 {
    x + 2
}

fn main() -> i32 {
    let a: i32 = apply(double, 5);
    let b: i32 = apply(add_two, a);
    if b != 12 { return 1; }
    0
}
