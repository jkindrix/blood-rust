struct Inner {
    value: i32,
}

struct Outer {
    inner: Inner,
    extra: i32,
}

fn get_value(o: Outer) -> i32 {
    o.inner.value + o.extra
}

fn main() -> i32 {
    let o = Outer { inner: Inner { value: 10 }, extra: 5 };
    get_value(o)
}
