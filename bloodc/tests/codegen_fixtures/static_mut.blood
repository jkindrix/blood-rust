static mut COUNTER: i32 = 0;

fn increment() -> () {
    @unsafe {
        COUNTER = COUNTER + 1;
    }
}

fn main() -> i32 {
    increment();
    increment();
    @unsafe {
        if COUNTER != 2 { return 1; }
    }
    0
}
