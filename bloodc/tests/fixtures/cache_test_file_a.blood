// Cache isolation test - File A
//
// This file and cache_test_file_b.blood have identically-named functions
// with identical signatures but different implementations. They test that
// the build cache correctly isolates artifacts by source file path.
//
// If cache contamination occurs, building both files in sequence would
// cause one file's compiled artifacts to be incorrectly used for the other.

fn compute_value() -> i32 {
    // File A returns 100
    100
}

fn main() {
    let result = compute_value();
    println!("File A result");
}
