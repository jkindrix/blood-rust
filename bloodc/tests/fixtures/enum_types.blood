// Types module for module_enum_struct_init.blood test

pub struct Nested {
    pub x: i32,
    pub y: i32,
}

pub enum TestResult {
    None,
    Success { value: i32 },
    Error { code: i32, nested: Nested },
}

pub struct Container {
    pub result: TestResult,
}

impl Container {
    pub fn new(result: TestResult) -> Container {
        Container { result: result }
    }
}
