// Module with an error - references undefined name
mod cross_file_error_base;

pub fn function_with_error() -> i32 {
    // This should report an error at THIS file, not the parent
    DoesNotExist
}
