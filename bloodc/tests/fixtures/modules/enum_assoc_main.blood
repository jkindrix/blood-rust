// Test cross-module associated functions on enums.

mod enum_associated_fns;

use enum_associated_fns.Color;
use enum_associated_fns.Value;

pub fn main() -> i32 {
    // Call associated function on imported enum (static method)
    let c: Color = Color::default_color();

    // Call associated function on imported enum (constructor-like)
    let v: Value = Value::number(42);
    let z: Value = Value::zero();

    // Return success indicator
    0
}
