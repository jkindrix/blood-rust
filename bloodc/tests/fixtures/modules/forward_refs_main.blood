// Main file that imports a module with forward references
mod forward_refs_child;

fn main() {
    let tree = forward_refs_child::create_tree(42);
    let node = forward_refs_child::create_node(100);
}
