// Module resolution test fixture: helper module with various exportable items.

pub struct Data {
    pub value: i32,
}

pub struct Point {
    pub x: i32,
    pub y: i32,
}

pub enum Kind {
    A,
    B(i32),
    C { x: i32, y: i32 },
}

pub type MyInt = i32;

pub const MAX_VALUE: i32 = 100;

pub fn get_value() -> i32 {
    42
}

impl Point {
    pub fn new(x: i32, y: i32) -> Point {
        Point { x, y }
    }

    pub fn origin() -> Point {
        Point { x: 0, y: 0 }
    }
}
