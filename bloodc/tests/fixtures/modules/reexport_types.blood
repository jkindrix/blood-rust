// Internal module that defines types.
// These types are re-exported by the parent module.

pub struct Point {
    pub x: i32,
    pub y: i32,
}

pub fn origin() -> Point {
    Point { x: 0, y: 0 }
}

pub enum Color {
    Red,
    Green,
    Blue,
}
