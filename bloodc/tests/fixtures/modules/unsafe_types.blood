// Types for testing @unsafe blocks with module-imported types

pub struct PtrHolder {
    pub ptr: *const u8,
    pub len: usize,
}

pub struct MutPtrHolder {
    pub ptr: *mut u8,
    pub cap: usize,
}
