// Test advanced use patterns with external modules.
// This tests associated functions, methods, and grouped imports.

mod helper;

// Import types to test associated functions
use helper.Point;
use helper.Kind;

pub fn main() -> i32 {
    // Test associated function on imported struct
    let p1: Point = Point::new(5, 10);
    let p2: Point = Point::origin();

    // Test enum variant construction with imported enum
    let k1: Kind = Kind::A;
    let k2: Kind = Kind::B(42);
    let k3: Kind = Kind::C { x: 1, y: 2 };

    // Return a value to verify types work
    p1.x + p2.x
}
