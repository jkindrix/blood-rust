// Test glob use statements for importing all public items from external modules.

mod helper;

// Glob import all public items (uses :: before *)
use helper::*;

pub fn main() -> i32 {
    let p: Point = Point::new(1, 2);
    let k: Kind = Kind::A;
    let v: i32 = get_value();
    let m: i32 = MAX_VALUE;
    let n: MyInt = 10;

    p.x + v + m + n
}
