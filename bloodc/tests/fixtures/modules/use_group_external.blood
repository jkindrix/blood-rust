// Test grouped use statements for importing from external modules.

mod helper;

// Group import multiple items (uses :: before {})
use helper::{Point, Kind, get_value, MAX_VALUE};

pub fn main() -> i32 {
    let p: Point = Point::new(1, 2);
    let k: Kind = Kind::A;
    let v: i32 = get_value();
    let m: i32 = MAX_VALUE;

    p.x + v + m
}
