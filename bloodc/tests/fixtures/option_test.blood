// Test Option methods that supposedly work

fn main() {
    let some_val: Option<i32> = Some(42);

    if some_val.is_some() {
        println!("Has value");
    }

    let val = some_val.unwrap();
    println!("Done");
}
