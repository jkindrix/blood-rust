
effect State {
    op get() -> i32;
}

deep handler GetterA for State {
    let val: i32
    return(x) { x }
    op get() { resume(val) }
}

fn main() -> i32 {
    with GetterA { val: 42 } handle {
        perform State.get()
    }
}
