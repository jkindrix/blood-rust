
effect State {
    op get() -> i32;
}

deep handler GetterB for State {
    let val: i32
    return(x) { x }
    op get() { resume(val) }
}

fn main() -> i32 {
    with GetterB { val: 99 } handle {
        perform State.get()
    }
}
