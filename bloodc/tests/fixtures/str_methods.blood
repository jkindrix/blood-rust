// Test &str methods: len, as_bytes, chars

pub fn test_str_len() -> usize {
    let s: &str = "hello";
    s.len()
}

pub fn test_str_len_chars() -> usize {
    let s: &str = "hello";
    s.len_chars()
}

pub fn test_str_as_bytes() {
    let s: &str = "hello";
    let bytes: &[u8] = s.as_bytes();
    // Just verify it compiles
}

pub fn test_str_chars() {
    let s: &str = "hello";
    let chars: Vec<char> = s.chars();
    // Just verify it compiles
}

pub fn test_str_char_at() {
    let s: &str = "hello";
    let idx: usize = 0;
    let c: Option<char> = s.char_at(idx);
}

pub fn main() -> i32 {
    let len = test_str_len();
    test_str_as_bytes();
    test_str_chars();
    test_str_char_at();
    0
}
