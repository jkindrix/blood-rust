// Test Vec.get() functionality

fn main() {
    let mut v: Vec<i32> = Vec::new();
    v.push(10);
    v.push(20);
    v.push(30);

    // Test get with valid index
    let maybe = v.get(1);
    println!("Got element from Vec");
}
