// Test Vec<T> indexing

fn test_vec_indexing(v: Vec<i32>) -> i32 {
    let first = v[0];
    let second = v[1];
    let i = 2;
    let third = v[i];
    first
}

fn test_vec_ref_indexing(v: &Vec<i32>) -> i32 {
    v[0]
}

fn test_vec_mut_ref_indexing(v: &mut Vec<i32>) -> i32 {
    v[0]
}

fn main() {
    // Test that Vec<T>[i] type-checks
}
