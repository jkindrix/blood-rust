// Test Vec<T> methods

fn test_vec_with_capacity() -> Vec<i32> {
    Vec::with_capacity(10)
}

fn test_vec_insert(v: &mut Vec<i32>, index: usize, val: i32) {
    v.insert(index, val);
}

fn test_vec_remove(v: &mut Vec<i32>, index: usize) -> i32 {
    v.remove(index)
}

fn test_vec_swap_remove(v: &mut Vec<i32>, index: usize) -> i32 {
    v.swap_remove(index)
}

fn test_vec_truncate(v: &mut Vec<i32>, len: usize) {
    v.truncate(len);
}

fn test_vec_first_mut(v: &mut Vec<i32>) -> Option<&mut i32> {
    v.first_mut()
}

fn test_vec_last_mut(v: &mut Vec<i32>) -> Option<&mut i32> {
    v.last_mut()
}

fn test_vec_get_mut(v: &mut Vec<i32>, i: usize) -> Option<&mut i32> {
    v.get_mut(i)
}

fn test_vec_reserve(v: &mut Vec<i32>, additional: usize) {
    v.reserve(additional);
}

fn test_vec_shrink_to_fit(v: &mut Vec<i32>) {
    v.shrink_to_fit();
}

fn test_vec_as_slice(v: &Vec<i32>) -> &[i32] {
    v.as_slice()
}

fn test_vec_as_mut_slice(v: &mut Vec<i32>) -> &mut [i32] {
    v.as_mut_slice()
}

fn test_vec_append(v: &mut Vec<i32>, other: &mut Vec<i32>) {
    v.append(other);
}

fn test_vec_extend_from_slice(v: &mut Vec<i32>, slice: &[i32]) {
    v.extend_from_slice(slice);
}

fn test_vec_dedup(v: &mut Vec<i32>) {
    v.dedup();
}

fn main() {
    let v = Vec::with_capacity(10);
}
