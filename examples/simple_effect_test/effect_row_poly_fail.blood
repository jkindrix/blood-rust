// Test: This should FAIL to type check
// A pure function cannot call an effectful function

effect Emit<T> {
    op emit(value: T) -> ();
}

fn emit_one() / {Emit<i32>} {
    perform Emit.emit(42);
}

// This should fail - no row variable and no handler
fn pure_wrapper() {
    emit_one();  // ERROR: Emit<i32> not handled
}

fn main() {
}
