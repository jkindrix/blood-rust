// Test: for-in loop over fixed-size arrays

fn main() {
    let arr: [i32; 5] = [1, 2, 3, 4, 5];
    let mut sum: i32 = 0;

    // Iterate over array
    for item in arr {
        sum = sum + item;
    }

    // sum should be 15
    println_int(sum);
}

fn println_int(n: i32) {
    // Builtin
}
