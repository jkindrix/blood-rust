// Test: basic arithmetic operations
// EXPECT: 42
// EXPECT: 10
// EXPECT: 56
// EXPECT: 7
// EXPECT: 3
// EXPECT: -5
fn main() -> i32 {
    // Addition
    let a: i32 = 20 + 22;
    println_int(a);

    // Subtraction
    let b: i32 = 30 - 20;
    println_int(b);

    // Multiplication
    let c: i32 = 8 * 7;
    println_int(c);

    // Division
    let d: i32 = 42 / 6;
    println_int(d);

    // Modulo
    let e: i32 = 23 % 10;
    println_int(e);

    // Negative numbers
    let f: i32 = 0 - 5;
    println_int(f);

    // Verify values via exit code
    if a != 42 { return 1; }
    if b != 10 { return 2; }
    if c != 56 { return 3; }
    if d != 7 { return 4; }
    if e != 3 { return 5; }
    if f != -5 { return 6; }
    0
}
