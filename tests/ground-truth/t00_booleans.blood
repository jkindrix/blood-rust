// Test: boolean operations and comparisons
// EXPECT: 1
// EXPECT: 0
// EXPECT: 1
// EXPECT: 1
fn main() -> i32 {
    // Boolean literals and comparisons
    let t: bool = true;
    let f: bool = false;

    // Logical AND
    if t && t {
        println_int(1);
    } else {
        println_int(0);
    }

    if t && f {
        println_int(1);
    } else {
        println_int(0);
    }

    // Logical OR
    if f || t {
        println_int(1);
    } else {
        println_int(0);
    }

    // Logical NOT
    if !f {
        println_int(1);
    } else {
        println_int(0);
    }

    // Comparison operators
    if 5 > 3 && 3 < 5 && 5 >= 5 && 5 <= 5 && 5 == 5 && 4 != 5 {
        0
    } else {
        1
    }
}
