// Test: array repeat syntax [value; count]
// EXPECT: 0
// EXPECT: 0
// EXPECT: 0
// EXPECT: 15
fn main() -> i32 {
    // Create array with repeated value
    let arr: [i32; 3] = [0; 3];
    println_int(arr[0]); // 0
    println_int(arr[1]); // 0
    println_int(arr[2]); // 0

    // Array repeat with non-zero value
    let fives: [i32; 3] = [5; 3];
    let sum: i32 = fives[0] + fives[1] + fives[2];
    println_int(sum); // 15
    if sum != 15 { return 1; }

    0
}
