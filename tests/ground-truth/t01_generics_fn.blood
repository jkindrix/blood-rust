// Test: generic functions (forall / type parameters)
// EXPECT: 42
// EXPECT: 10
fn identity<T>(x: T) -> T {
    x
}

fn first<A, B>(a: A, b: B) -> A {
    a
}

fn main() -> i32 {
    let x: i32 = identity(42);
    println_int(x);

    let y: i32 = first(10, 20);
    println_int(y);

    if x != 42 { return 1; }
    if y != 10 { return 2; }
    0
}
