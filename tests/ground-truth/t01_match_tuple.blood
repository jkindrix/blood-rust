// Test: tuple creation and pattern matching
// EXPECT: 30
fn add_pair(t: (i32, i32)) -> i32 {
    match t {
        (a, b) => a + b,
    }
}

fn main() -> i32 {
    let pair: (i32, i32) = (10, 20);
    let sum: i32 = add_pair(pair);
    println_int(sum);

    if sum != 30 { return 1; }
    0
}
