// Test: mutable references
// EXPECT: 10
// EXPECT: 20
fn main() -> i32 {
    // Create a mutable variable and take a mutable reference
    let mut x: i32 = 10;
    println_int(x); // 10

    let r: &mut i32 = &mut x;
    *r = 20;

    println_int(x); // 20
    if x != 20 { return 1; }

    0
}
