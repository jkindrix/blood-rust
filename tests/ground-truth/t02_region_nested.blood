// Test: nested region blocks
// EXPECT: 10
// EXPECT: 30
fn main() -> i32 {
    let x: i32 = region {
        let outer: i32 = 10;
        let inner: i32 = region {
            let nested: i32 = outer * 3;
            nested
        };
        println_int(outer);
        inner
    };
    println_int(x);

    if x != 30 { return 1; }
    0
}
