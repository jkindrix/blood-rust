// Test: region blocks with unit return (side-effect only)
// EXPECT: 77
fn main() -> i32 {
    let mut x: i32 = 0;

    // Region block with unit return
    region {
        let a: i32 = 77;
        x = a;
    };

    println_int(x);

    if x != 77 { return 1; }
    0
}
