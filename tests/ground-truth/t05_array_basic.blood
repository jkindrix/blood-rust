// Test: array literals and indexing (if supported)
// EXPECT: 6
fn test_array(arr: [i32; 3]) -> i32 {
    match arr {
        [a, b, c] => a + b + c,
    }
}

fn main() -> i32 {
    let arr: [i32; 3] = [1, 2, 3];
    let sum: i32 = test_array(arr);
    println_int(sum);

    if sum != 6 { return 1; }
    0
}
