// Test: assert_eq_bool builtin
// EXPECT: 1
fn main() -> i32 {
    // Should not panic
    assert_eq_bool(true, true);
    assert_eq_bool(false, false);

    // Verify we got past the assertions
    println_int(1); // 1

    0
}
