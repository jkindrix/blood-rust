// Test: block expressions (braces producing a value)
// EXPECT: 42
fn main() -> i32 {
    let x: i32 = {
        let a: i32 = 20;
        let b: i32 = 22;
        a + b
    };
    println_int(x);

    if x != 42 { return 1; }
    0
}
