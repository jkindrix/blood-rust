// Test: basic lifetime parameter
// EXPECT: 20
fn longest<'a>(x: &'a i32, y: &'a i32) -> &'a i32 {
    if *x > *y { x } else { y }
}

fn main() -> i32 {
    let a: i32 = 10;
    let b: i32 = 20;
    let r: &i32 = longest(&a, &b);
    println_int(*r);
    0
}
