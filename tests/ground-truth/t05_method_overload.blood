// Test: multiple dispatch and method overloading
struct Printer {}

impl Printer {
    fn print(self, x: i32) -> () {
        println_int(x);
    }
    fn print(self, x: bool) -> () {
        println_bool(x);
    }
}

fn main() -> i32 {
    let p: Printer = Printer {};
    p.print(42);
    p.print(true);
    0
}
