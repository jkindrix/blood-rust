// Test: named arguments in function calls
// EXPECT: 8
// EXPECT: -2
fn rectangle_area(width: i32, height: i32) -> i32 {
    width * height
}

fn subtract(a: i32, b: i32) -> i32 {
    a - b
}

fn main() -> i32 {
    let area: i32 = rectangle_area(width: 2, height: 4);
    println_int(area);
    if area != 8 { return 1; }

    let diff: i32 = subtract(a: 3, b: 5);
    println_int(diff);
    if diff != -2 { return 2; }

    0
}
