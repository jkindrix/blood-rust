// Test: panic! macro causes program abort with non-zero exit
// EXPECT_EXIT: nonzero
// EXPECT: before panic
fn main() -> i32 {
    println_str("before panic");
    panic!("test panic message");
    // Should never reach here
    0
}
