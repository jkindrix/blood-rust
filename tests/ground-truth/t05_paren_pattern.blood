// Test: parenthesized pattern in match should work
fn main() -> i32 {
    let x: i32 = 5;
    let result: i32 = match x {
        (1) => 10,
        (y) => y,
    };
    println!("{}", result);
    0
}
// EXPECT: 5
