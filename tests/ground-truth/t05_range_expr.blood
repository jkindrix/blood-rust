// Test: range expressions as values
// EXPECT: 10
// EXPECT: 55
fn main() -> i32 {
    // Use range in for loop
    let mut count: i32 = 0;
    for i in 0..10 {
        count = count + 1;
    }
    println_int(count); // 10
    if count != 10 { return 1; }

    // Range with computation
    let mut sum: i32 = 0;
    for i in 1..11 {
        sum = sum + i;
    }
    println_int(sum); // 55
    if sum != 55 { return 2; }

    0
}
