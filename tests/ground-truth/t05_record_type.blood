// Test: anonymous record type in function signature
// EXPECT: 3
fn make_point(x: i32, y: i32) -> { x: i32, y: i32 } {
    { x: x, y: y }
}

fn main() -> i32 {
    let p = make_point(3, 4);
    println_int(p.x);
    0
}
