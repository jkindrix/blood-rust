// Test: static mutable item
// EXPECT: 2
static mut COUNTER: i32 = 0;

fn increment() -> () {
    @unsafe {
        COUNTER = COUNTER + 1;
    }
}

fn main() -> i32 {
    increment();
    increment();
    @unsafe {
        println_int(COUNTER);
    }
    0
}
