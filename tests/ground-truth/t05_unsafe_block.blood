// Test: unsafe block (@unsafe in Blood syntax)
// EXPECT: 42
fn main() -> i32 {
    // Basic unsafe block
    let result: i32 = @unsafe {
        // Unsafe operations allowed inside
        let x: i32 = 42;
        x
    };
    println_int(result); // 42
    if result != 42 { return 1; }

    0
}
