// EXPECT: 0
// Test: user-defined macro declaration
macro my_assert {
    ($cond:expr) => {
        if !$cond {
            panic!("assertion failed");
        }
    };
}

fn main() -> i32 {
    my_assert!(true);
    0
}
