// Test: user-defined macro declaration
// No output expected; test passes if compilation and execution succeed
macro my_assert {
    ($cond:expr) => {
        if !$cond {
            panic!("assertion failed");
        }
    };
}

fn main() -> i32 {
    my_assert!(true);
    0
}
