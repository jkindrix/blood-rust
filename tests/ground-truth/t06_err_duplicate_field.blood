// COMPILE_FAIL: duplicate field name in struct
// Test: struct with duplicate field names should fail
struct Bad {
    x: i32,
    y: i32,
    x: i32,
}

fn main() -> i32 {
    let b: Bad = Bad { x: 1, y: 2, x: 3 };
    0
}
