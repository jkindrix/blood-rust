// XFAIL: duplicate method detection not yet implemented
// Test: two methods with the same name in impl block should fail
struct Foo {
    x: i32,
}

impl Foo {
    fn get(self) -> i32 { self.x }
    fn get(self) -> i32 { self.x + 1 }
}

fn main() -> i32 {
    0
}
