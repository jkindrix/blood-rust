// COMPILE_FAIL: mismatched types in if/else branches
// Test: if/else branches with different types should fail when used as expression
fn main() -> i32 {
    let x: i32 = if true { 42 } else { false };
    0
}
