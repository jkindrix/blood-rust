// COMPILE_FAIL: non-path expression in effect annotation (E0100)
// Test: non-path expression in effect annotation should fail
fn uses_effect() -> i32 / {42} {
    0
}

fn main() -> i32 {
    uses_effect()
}
