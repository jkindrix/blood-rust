// COMPILE_FAIL: unknown operation
// Test: handler with mismatched operation name should fail (E0305)
effect Foo {
    op bar() -> i32;
}

deep handler BadHandler for Foo {
    return(x) { x }
    op nonexistent() { resume(42) }
}

fn main() -> i32 {
    0
}
