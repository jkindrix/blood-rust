// COMPILE_FAIL: E0804
// Test: consuming linear value inside a loop should fail
fn main() -> i32 {
    let x: linear i32 = 42;
    let mut i: i32 = 0;
    while i < 3 {
        let y: linear i32 = x; // ERROR: linear value consumed in loop (E0804)
        i = i + 1;
    }
    0
}
