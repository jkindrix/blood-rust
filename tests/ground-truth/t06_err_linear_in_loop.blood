// XFAIL: linear value in loop check not yet implemented
// Test: consuming linear value inside a loop should fail
fn main() -> i32 {
    let x: linear i32 = 42;
    let mut i: i32 = 0;
    while i < 3 {
        let y: i32 = x;
        i = i + 1;
    }
    0
}
