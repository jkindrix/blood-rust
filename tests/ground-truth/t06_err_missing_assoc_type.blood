// COMPILE_FAIL: missing associated type in trait impl
// Test: impl missing required associated type should fail (E0233)
trait Container {
    type Item;
    fn get(self) -> i32;
}

struct Box {
    value: i32,
}

impl Container for Box {
    fn get(self) -> i32 { self.value }
    // Missing: type Item = ...;
}

fn main() -> i32 {
    0
}
