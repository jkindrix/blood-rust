// COMPILE_FAIL: non-exhaustive match
// Test: match expression that doesn't cover all variants should fail
enum Direction {
    North,
    South,
    East,
    West,
}

fn describe(d: Direction) -> i32 {
    match d {
        Direction::North => 1,
        Direction::South => 2,
        // Missing East and West
    }
}

fn main() -> i32 {
    let x: i32 = describe(Direction::North);
    0
}
