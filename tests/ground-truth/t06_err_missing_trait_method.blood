// COMPILE_FAIL: missing trait method implementation
// Test: impl block missing a required trait method should fail
trait Shape {
    fn area(self) -> i32;
    fn perimeter(self) -> i32;
}

struct Rect {
    w: i32,
    h: i32,
}

impl Shape for Rect {
    fn area(self) -> i32 { self.w * self.h }
}

fn main() -> i32 {
    0
}
