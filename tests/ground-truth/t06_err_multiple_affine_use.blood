// COMPILE_FAIL: E0803
// Test: affine value used more than once should fail
fn main() -> i32 {
    let t: affine i32 = 42;
    let a: affine i32 = t; // First use (ok for affine)
    let b: affine i32 = t; // ERROR: affine already used (E0803)
    0
}
