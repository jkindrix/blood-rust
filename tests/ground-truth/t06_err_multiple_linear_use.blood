// COMPILE_FAIL: E0802
// Test: using a linear value more than once should fail (E0802)
// The linear `x` is used twice - this is an error
fn main() -> i32 {
    let x: linear i32 = 42;
    let a: linear i32 = x; // First use of x (consumes it)
    let b: linear i32 = x; // ERROR: x already used (E0802)
    0
}
