// COMPILE_FAIL: no method greet found for Dog (E0231)
// Test: calling a method on type that doesn't implement the trait should fail
struct Cat {}
struct Dog {}

trait Greet {
    fn greet(self) -> i32;
}

impl Greet for Cat {
    fn greet(self) -> i32 { 1 }
}

fn main() -> i32 {
    let d: Dog = Dog {};
    d.greet()
}
