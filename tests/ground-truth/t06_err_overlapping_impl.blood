// COMPILE_FAIL: overlapping impl blocks
// Test: implementing same trait twice for same type should fail
trait Foo {
    fn foo(self) -> i32;
}

struct Bar {
    x: i32,
}

impl Foo for Bar {
    fn foo(self) -> i32 { self.x }
}

impl Foo for Bar {
    fn foo(self) -> i32 { self.x + 1 }
}

fn main() -> i32 {
    0
}
