// COMPILE_FAIL: pattern type mismatch
// Test: slice pattern with more elements than array size should fail (E0226)
fn main() -> i32 {
    let arr: [i32; 2] = [1, 2];
    match arr {
        [a, b, c] => a + b + c,
    }
}
