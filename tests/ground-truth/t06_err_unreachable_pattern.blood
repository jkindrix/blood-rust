// COMPILE_FAIL: unreachable pattern
// Test: unreachable pattern after catch-all should be an error
fn main() -> i32 {
    let x: i32 = 5;
    match x {
        _ => println_int(0),
        5 => println_int(5),
    }
    0
}
