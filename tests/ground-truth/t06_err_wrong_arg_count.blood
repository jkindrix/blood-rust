// COMPILE_FAIL: wrong number of arguments
// Test: calling a function with wrong number of arguments should fail
fn add(a: i32, b: i32) -> i32 {
    a + b
}

fn main() -> i32 {
    let x: i32 = add(1, 2, 3);
    0
}
