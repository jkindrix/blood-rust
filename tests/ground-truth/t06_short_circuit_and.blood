// Test: && short-circuit evaluation (BUG-011)
// Verifies that the RHS of && is not evaluated when LHS is false.
// EXPECT: PASS
fn side_effect_and() -> bool { print("FAIL"); true }
fn main() -> i32 {
    if false && side_effect_and() { print("BAD"); }
    print("PASS\n");
    0
}
