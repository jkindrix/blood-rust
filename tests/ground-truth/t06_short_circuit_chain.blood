// Test: chained && short-circuit (BUG-011)
// Verifies that false && a() && b() evaluates neither a nor b.
// EXPECT: PASS
fn effect_a() -> bool { print("A"); true }
fn effect_b() -> bool { print("B"); true }
fn main() -> i32 {
    if false && effect_a() && effect_b() { }
    print("PASS\n");
    0
}
