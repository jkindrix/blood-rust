// Test: || short-circuit evaluation (BUG-011)
// Verifies that the RHS of || is not evaluated when LHS is true.
// EXPECT: PASS
fn side_effect_or() -> bool { print("FAIL"); false }
fn main() -> i32 {
    if true || side_effect_or() { }
    print("PASS\n");
    0
}
